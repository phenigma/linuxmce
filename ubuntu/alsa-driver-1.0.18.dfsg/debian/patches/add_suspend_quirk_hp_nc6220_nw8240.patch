Index: alsa-driver-1.0.16/sound/pci/intel8x0.c
===================================================================
--- alsa-driver-1.0.16.orig/sound/pci/intel8x0.c	2008-03-04 21:49:06.000000000 -0500
+++ alsa-driver-1.0.16/sound/pci/intel8x0.c	2008-03-04 21:53:41.000000000 -0500
@@ -2527,6 +2527,13 @@
 	 * after S2RAM.  So, don't touch it.
 	 */
 	/* pci_set_power_state(pci, pci_choose_state(pci, state)); */
+
+	/* Quirk support for HP nw8240 and HP NC6220, LP #151111 */
+	if ((pci->subsystem_vendor == 0x103c) &&
+	    ((pci->subsystem_device == 0x0934) ||
+	    (pci->subsystem_device == 0x0944))) {
+		pci_set_power_state(pci, pci_choose_state(pci, state));
+	}
 	return 0;
 }
 
