Index: mythplugins-0.22/mythweb/modules/tv/handler.php
===================================================================
--- mythplugins-0.22.orig/mythweb/modules/tv/handler.php	2009-01-17 10:05:29.000000000 +0100
+++ mythplugins-0.22/mythweb/modules/tv/handler.php	2009-11-11 18:52:22.000000000 +0100
@@ -18,11 +18,14 @@
         custom_error('Error creating data/tv_icons: Please check permissions on the data directory.');
         exit;
     }
+
+/* Not needed in Debian
     if (!is_writable('data/tv_icons')) {
         $process_user = posix_getpwuid(posix_geteuid());
         custom_error('data/tv_icons directory is not writable by '.$process_user['name'].'. Please check permissions.');
         exit;
     }
+*/
 
 // Call the opensearch module early, before loading all kinds of stuff it
 // doesn't need.  Plus, it's not "enabled" like other modules, so we skip that
Index: mythplugins-0.22/mythweb/includes/data_dir.php
===================================================================
--- mythplugins-0.22.orig/mythweb/includes/data_dir.php	2009-01-17 10:05:29.000000000 +0100
+++ mythplugins-0.22/mythweb/includes/data_dir.php	2009-11-11 18:53:11.000000000 +0100
@@ -17,7 +17,10 @@
     if (!is_dir('data') && !mkdir('data', 0755)) {
         custom_error('Error creating the data directory. Please check permissions.');
     }
+
+/* Not needed in Debian
     if (!is_writable('data')) {
         $process_user = posix_getpwuid(posix_geteuid());
         custom_error('data directory is not writable by '.$process_user['name'].'. Please check permissions.');
     }
+*/
