#! /bin/sh
DOCDIR=/opt/libxine-pluto/share/doc/libxine1-console
if [ -d "$DOCDIR" -a ! -L "$DOCDIR" ]; then                  
  rmdir "$DOCDIR" && ln -s libxine1-pluto-bin "$DOCDIR"
fi

##DEBHELPER##
