# Defaults for shairport initscript
# sourced by /etc/init.d/shairport
# installed at /etc/default/shairport by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
RUN=no
SQUEEZEBOX=no
