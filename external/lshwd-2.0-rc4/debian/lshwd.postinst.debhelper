# Automatically added by dh_installinit
if [ -x "/etc/init.d/lshwd" ]; then
	update-rc.d lshwd start 40 S . >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d lshwd start || exit 0
	else
		/etc/init.d/lshwd start || exit 0
	fi
fi
# End automatically added section
