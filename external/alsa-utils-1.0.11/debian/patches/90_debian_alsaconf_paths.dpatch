#!/bin/sh -e
##
## 90_debian_alsaconf.dpatch by Thomas Hood
##
## All lines beginning with `## DP:' are a description of the patch.
## DP: Don't precompute paths for lspci and lsmod


if [ $# -ne 1 ]; then
    echo >&2 "`basename $0`: script expects -patch|-unpatch as argument"
    exit 1
fi

[ -f debian/patches/00patch-opts ] && . debian/patches/00patch-opts
patch_opts="${patch_opts:--f --no-backup-if-mismatch}"

case "$1" in
       -patch) patch $patch_opts -p1 < $0;;
       -unpatch) patch $patch_opts -p1 -R < $0;;
        *)
                echo >&2 "`basename $0`: script expects -patch|-unpatch as argument"
                exit 1;;
esac

exit 0

@DPATCH@
diff -urNad a/alsaconf/alsaconf.in b/alsaconf/alsaconf.in
--- a/alsaconf/alsaconf.in	2006-03-25 20:34:12.403315175 +0100
+++ b/alsaconf/alsaconf.in	2006-03-25 20:35:56.297201375 +0100
@@ -69,12 +69,18 @@
   distribution="unknown"
 fi
 
+if false; then
 for prog in lspci lsmod; do
 	for path in /sbin /usr/sbin /bin /usr/bin;do
 		test -x $path/$prog && eval $prog=$path/$prog
 	done
 done
 unset prog path
+else
+# debian patch
+lspci=lspci
+lsmod=lsmod
+fi
 
 usage() {
     xecho "ALSA configurator"
