#! /bin/sh -e

#DEBHELPER#

if [ "$1" = "remove" ]; then
    update-alternatives --remove x-window-manager /usr/bin/icewm-experimental
    update-alternatives --remove x-session-manager /usr/bin/icewm-session-experimental
fi

exit 0

