AM_CPPFLAGS=-D_REENTRANT -D_FILE_OFFSET_BITS=64
bin_PROGRAMS=pluto-fuse-smb
pluto_fuse_smb_SOURCES=pluto-fuse-smb.c
noinst_HEADERS = 

EXTRA_DIST= debian pluto-fuse-smb.1

#  if you write a self-test script named `chk', uncomment the
#  following and add `chk' to the EXTRA_DIST list
#TESTS=chk

#  build and install the .info pages
#info_TEXINFOS = pluto_fuse_smb.texinfo
#pluto_fuse_smb_TEXINFOS = gpl.texinfo

#  install the man pages
man_MANS=pluto-fuse-smb.1
