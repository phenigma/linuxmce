#! /bin/sh -e

#DEBHELPER#

if [ "$1" = "remove" ]; then
   update-alternatives --remove x-window-manager /usr/bin/icewm
   update-alternatives --remove x-session-manager /usr/bin/icewm-session
fi

exit 0

