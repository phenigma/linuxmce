/*!
 *\qmlmodule lib
 *
 *This is a list of the handlers included in the 'lib' section of qml. These are universal handlers that can be used to build a
 *component that is tied to a LinuxMCE function quickly
 */
