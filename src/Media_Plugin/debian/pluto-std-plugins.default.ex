# Defaults for pluto-std-plugins initscript
# sourced by /etc/init.d/pluto-std-plugins
# installed at /etc/default/pluto-std-plugins by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
