ROOT=..

CPPFLAGS=-I. -I..
CXXFLAGS=-Os -W -Wall
LDFLAGS=-L$(ROOT)/lib
LDLIBS=-lPlutoUtils -lDCECommon -lpthread -lSerializeClass

ifeq ($(ARCH),mipsel)
	CXX=mipsel-linux-g++
else
	CXX=g++
endif

all: pluto-lzo
	cp pluto-lzo $(ROOT)/bin

clean:
	rm -f pluto-lzo
