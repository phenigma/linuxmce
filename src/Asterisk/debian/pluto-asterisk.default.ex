# Defaults for pluto-asterisk initscript
# sourced by /etc/init.d/pluto-asterisk
# installed at /etc/default/pluto-asterisk by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
