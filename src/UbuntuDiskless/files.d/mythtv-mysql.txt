#!/bin/bash

set -e 

. ./filesd-parms.sh $@

File="/etc/mythtv/mysql.txt"

if [[ -f "$File" ]] ;then
	kdir -p "${Parm_RootLocation}/$(dirname $File)"
	p "${File}" "${Parm_RootLocation}/${File}"
	ed -i "s/^DBHostName.*/DBHostName=dcerouter/g" "${Parm_RootLocation}/${File}"
fi
