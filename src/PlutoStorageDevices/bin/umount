#!/bin/bash -e

. /usr/pluto/bin/LockUtils.sh

echo				   >> /var/log/pluto/umount-wrapper.log
echo "$$ CALLED TO 'umount $@'"    >> /var/log/pluto/umount-wrapper.log
WaitLock "umount" "umount-wrapper" >> /var/log/pluto/umount-wrapper.log

/bin/umount.wrapped $@ &

sleep 5

