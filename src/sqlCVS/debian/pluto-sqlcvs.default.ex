# Defaults for pluto-sqlcvs initscript
# sourced by /etc/init.d/pluto-sqlcvs
# installed at /etc/default/pluto-sqlcvs by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
