# Defaults for pluto-slim-server-streamer initscript
# sourced by /etc/init.d/pluto-slim-server-streamer
# installed at /etc/default/pluto-slim-server-streamer by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
