# Defaults for pluto-default-tftpboot initscript
# sourced by /etc/init.d/pluto-default-tftpboot
# installed at /etc/default/pluto-default-tftpboot by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
