#!/bin/make

none:


clean:


install:
	find . -iname "*.mp3" -exec install -m644 -D {} $(DESTDIR)/{} \;
	find . -iname "*.wav" -exec install -m644 -D {} $(DESTDIR)/{} \;
	find . -iname "*.ac3" -exec install -m644 -D {} $(DESTDIR)/{} \;
